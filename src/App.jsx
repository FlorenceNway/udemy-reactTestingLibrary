import React from 'react';
import PropTypes from 'prop-types';

const app = props => {
    return (
        <div className='App'>
            Hello World
        </div>
    );
};

app.propTypes = {
    
};

export default app;